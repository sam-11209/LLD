package com.system.lld.elevator;
/**
 * Direction.java
 *
 * An enum representing the possible movement directions of an elevator.
 * IDLE means the elevator is not moving (waiting for requests).
 */
public enum Direction {
    UP,
    DOWN,
    IDLE
}
